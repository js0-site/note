#!/usr/bin/env -S node --trace-uncaught --expose-gc --unhandled-rejections=strict --experimental-wasm-modules
import hack from './index.js';

import CoffeeScript from "coffeescript";

hack(CoffeeScript);

export default CoffeeScript.compile;
