var COMMONJS;

import jsext from './jsext.js';

export var resolve = jsext;

COMMONJS = {
  format: "commonjs",
  shortCircuit: true
};

export const load = (url, context, defaultLoad) => {
  if (url.endsWith(".node")) {
    return COMMONJS;
  }
  return defaultLoad(url, context);
};
